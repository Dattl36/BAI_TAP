databases:
  - name: salon-db
    databaseName: salon
    user: salon_user

services:
  - type: web
    name: salon-backend
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn salon_backend.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: salon-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: EMAIL_HOST_USER
        value: "" # Please fill this in Render Dashboard
      - key: EMAIL_HOST_PASSWORD
        value: "" # Please fill this in Render Dashboard
